# Issue Tracker

This project uses **Local Markdown** for issue tracking.

- **Location**: `.scratch/`
- **Format**: Markdown files with YAML frontmatter.
- **Workflow**: Create a file in `.scratch/` to initiate a task. Use the `status` field in the frontmatter to track triage state.
